server:
  port: 8080

spring:

  application:
    name: PAYMENT_SYSTEM

  datasource:
    url: jdbc:mysql://localhost:3306/banking_db
    username: root
    password: junl1234

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  jackson:
    serialization:
      indent-output: true

jwt:
  secret: mySuperSecureJwtSecretKeyForBankingProject123456789
  expiration: 86400000

logging:
  level:
    org.springframework.security: DEBUG